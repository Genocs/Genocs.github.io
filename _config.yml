theme: jekyll-theme-cayman
title: Genocs
author: Nocco Giovanni Emanuele
email: info@genocs.com
description: >-
  Field notes on turning an idea into software — architecture, delivery, and the right mindset.
# social links
twitter_username: NoccoGiovanni # DO NOT include the @ character, or else the build will fail!
github_username:  giovanninocco # DO NOT include the @ character, or else the build will fail!

show_excerpts: false # set to false to remove excerpts on the homepage

defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: ""
      type: "posts"
    values:
      layout: post
